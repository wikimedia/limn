define ['elements/Base'], (BaseElement) ->
    class GroupElement extends BaseElement
        
        build: (parentElement) ->
            @sel = parentElement.append 'g'
            @renderChildren @sel
