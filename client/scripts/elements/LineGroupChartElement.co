define ['elements/GroupElement'], (GroupElement) ->
    class LineGroupChartElement extends GroupElement
        
        build: (parentElement) ->
            @sel = parentElement.append 'g'
                .classed 'metricVisualizations', true
            @renderChildren @sel
