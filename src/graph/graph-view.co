{ View,
} = require '../base/view'
{ Graph,
} = require './graph-model'

class exports.GraphView extends View
    template: 'graph'
    
    (idOrModel, action) ->
        super()
        if idOrModel instanceof Graph
            @model idOrModel
        else
            @model new Graph id: idOrModel
            @model().fetch()
