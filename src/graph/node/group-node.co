_  = require 'underscore'
ko = require 'knockout'

{ GraphNode,
} = require './graph-node'


/**
 * @class
 * @extends GraphNode
 */
class exports.GroupNode extends GraphNode
    nodeType : 'group'
    @registerType()
    
    -> super ...
    
    
    determineParent: (node) ->
        @$el if @el() and @$el
