d3 = require 'd3'
Seq = require 'seq'

{ _, op,
} = limn = require 'limn/client'
{ ChartElementData,
} = require '../chart-element-data'
{ GroupElement,
} = require '../group-element'


/**
 * @class
 * @extends ChartElement
 */
LineGroupChartElement = exports.LineGroupChartElement = GroupElement.extend do # {{{
    __bind__       : <[  ]>
    typeName       : 'LineGroupChartElement'
    tagName        : 'g'
    className      : 'metricVisualizations'
    
    
    constructor : function LineGroupChartElement
        GroupElement ...
    
    initialize : ->
        GroupElement::initialize ...
    
    
    
# }}}
